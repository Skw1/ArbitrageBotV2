/*
  // Example Ticker 
  const ticker = 'TRON usdt';

  // Spot Symbols 
  const mexcSpotSymbol = ticker.toUpperCase().replace(' ','').replace('TRON','TRX'); // BTCUSDT
  const lbankSpotSymbol = ticker.toLowerCase().replace('usdt', '_usdt').replace(' ','').replace('tron','trx'); // btc_usdt
  const bybitSpotSymbol = ticker.toUpperCase().replace(' ','').replace('TRON','TRX'); // BTCUSDT
  const kucoinSpotSymbol = ticker.toUpperCase().replace(' ','').replace('TRON','TRX').replace('USDT', '-USDT'); // BTC-USDT
  const ourbitSpotSymbol = ticker.toUpperCase().replace(' ','').replace('TRON','TRX'); // BTCUSDT // ?
  const bitunixSpotSymbol = ticker.toUpperCase().replace(' ','').replace('TRON','TRX'); // BTCUSDT

  // Futures Symbols 
  const mexcFuturesSymbol = ticker.toUpperCase().replace(' ','').replace('TRON','TRX').replace('USDT', '_USDT'); // BTC_USDT
  const lbankFuturesSymbol = ticker.toUpperCase().replace(' ','').replace('TRON','TRX').replace('USDT', '_USDT'); // BTC_USDT
  const bybitFuturesSymbol = ticker.toUpperCase().replace(' ','').replace('TRON','TRX'); // BTCUSDT
  const kucoinFuturesSymbol = ticker.toUpperCase().replace(' ','').replace('TRON','TRX').replace('USDT', 'USDTM'); // BTCUSDTM
  const ourbitFuturesSymbol = ticker.toUpperCase().replace(' ','').replace('TRON','TRX'); // BTCUSDT // ?
  const bitunixFuturesSymbol = ticker.toUpperCase().replace(' ','').replace('TRON','TRX'); // BTCUSDT


  // Console Log
  console.log("mexcSpotSymbol" ,  mexcSpotSymbol);
  console.log("mexcFuturesSymbol" ,  mexcFuturesSymbol);

  console.log("lbankSpotSymbol" ,  lbankSpotSymbol);
  console.log("lbankFuturesSymbol" ,  lbankFuturesSymbol);

  console.log("bybitSpotSymbol" ,  bybitSpotSymbol);
  console.log("bybitFuturesSymbol" ,  bybitFuturesSymbol);

  console.log("kucoinSpotSymbol" ,  kucoinSpotSymbol);
  console.log("kucoinFuturesSymbol" ,  kucoinFuturesSymbol);

  console.log("ourbitSpotSymbol" ,  ourbitSpotSymbol);
  console.log("ourbitFuturesSymbol" ,  ourbitFuturesSymbol);

  console.log("bitunixSpotSymbol" ,  bitunixSpotSymbol);
  console.log("bitunixFuturesSymbol" ,  bitunixFuturesSymbol);

*/